FROM baseImage

ARG ENVIRONMENT
ARG SERVICE
ARG BRANCH_NAME
ARG COMMIT_HASH
ARG BUILD_DATE

ENV ENVIRONMENT=$ENVIRONMENT \
    SERVICE=$SERVICE \
    BRANCH_NAME=$BRANCH_NAME \
    COMMIT_HASH=$COMMIT_HASH \
    BUILD_DATE=$BUILD_DATE

WORKDIR /app


COPY . .


EXPOSE 8000

CMD [ "./run.sh" ]